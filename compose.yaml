services:
  postgres:
    image: 'postgres:16'
    container_name: studentms-db
    environment:
      - 'POSTGRES_DB=studentms'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=studentms'
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U studentms"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
